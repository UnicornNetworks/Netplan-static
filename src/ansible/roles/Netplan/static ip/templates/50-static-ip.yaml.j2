network:
  version: 2
  renderer: networkd
  ethernets:
    {{vagrant_interface}}:
      dhcp4: no
      addresses: [{{ip}}/{{netmask_bits}}]
      routes:
      - to: default
        via: {{gateway}}
      nameservers:
        addresses: [{{nameserver0}},{{nameserver1}}]